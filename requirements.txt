# Core (ROI loading + targets). For extras use the pyproject optional groups:
#   pip install -e ".[color]"   # color experiments (sklearn, torch, matplotlib)
#   pip install -e ".[clip]"    # CLIP embedding targets (open_clip, torch)
#   pip install -e ".[rawnsd]"  # raw-NSD fine-grained ROIs (nibabel)
#   pip install -e ".[all]"     # everything
numpy>=1.24
h5py>=3.8
huggingface_hub>=0.23
requests>=2.28
pandas>=2.0
pillow>=10.0
